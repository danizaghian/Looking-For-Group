<a name="top"></a>
<div class="jumbotron genres">
  <div class = "jumbotron-text"><%= @genre.name%></div>
</div>

ALL GAMES IN THIS GENRE:<br>
<% @genre.games.each do |game| %>
  <div class = "card-container">
  <div class="demo-card-square mdl-card mdl-shadow--2dp">
  <a href = "<%= game_path(game.slug)%>">
    <div class="mdl-card__title mdl-card--expand">
      <h2 class="mdl-card__title-text"><%= game.name %></h2>
    </div>
  </a>

    <div class="mdl-card__supporting-text">
     Genres:
     <% i = 0 %>
     <% length = game.genres.length %>
     <% game.genres.each do |genre| %>
      <%= link_to genre.name, genre_path(genre.slug)%><% i+= 1%><% if i < (length) %>,<% end %>
    <% end %>  <br>
    <%= game.description %> 
    </div>

  </div>
  </div>
<% end %>


<a href="#top" class="btt mdl-button mdl-button--colored mdl-js-button mdl-js-ripple-effect">
  Back to Top</a>
